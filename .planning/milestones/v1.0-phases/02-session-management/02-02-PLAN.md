---
phase: 02-session-management
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - claudeos-sessions/package.json
  - claudeos-sessions/src/sidebar/session-tree.ts
  - claudeos-sessions/src/sidebar/session-item.ts
  - claudeos-sessions/src/sidebar/group-item.ts
  - claudeos-sessions/test/sidebar/session-tree.test.ts
  - claudeos-sessions/test/sidebar/session-item.test.ts
autonomous: true
requirements:
  - SES-01
  - SES-03
  - SES-05
  - SES-06
  - SES-08
  - SES-09

must_haves:
  truths:
    - "Sidebar shows sessions grouped by status: Active, Idle, Waiting, Stopped as expanded sections"
    - "Archive and Zombie sections appear at bottom, collapsed by default, with badge count on headers"
    - "Each session shows correct status icon: sync~spin for active, debug-pause for idle, question for waiting, stop-circle for stopped"
    - "Unread sessions render with highlighted (bold) label; read sessions render normal"
    - "Zombie sessions show error-colored icon with time-ago description"
    - "Archived sessions show archive icon with archived-date description"
    - "TreeView.badge shows total count of waiting sessions"
    - "Sessions are sorted most-recent-first within each status group"
  artifacts:
    - path: "claudeos-sessions/src/sidebar/session-tree.ts"
      provides: "TreeDataProvider for sessions sidebar"
      exports: ["SessionTreeProvider"]
    - path: "claudeos-sessions/src/sidebar/session-item.ts"
      provides: "TreeItem factory for individual sessions"
      exports: ["createSessionItem", "STATUS_ICONS"]
    - path: "claudeos-sessions/src/sidebar/group-item.ts"
      provides: "TreeItem factory for status group headers"
      exports: ["createGroupItem"]
    - path: "claudeos-sessions/package.json"
      provides: "Extension manifest with viewsContainers, views, viewsWelcome, commands, menus, keybindings"
      contains: "claudeos.sessions"
  key_links:
    - from: "claudeos-sessions/src/sidebar/session-tree.ts"
      to: "claudeos-sessions/src/state/session-store.ts"
      via: "SessionStore.getSessionsByStatus() called in getChildren()"
      pattern: "sessionStore.*getSessionsByStatus"
    - from: "claudeos-sessions/src/sidebar/session-tree.ts"
      to: "vscode.TreeDataProvider"
      via: "implements TreeDataProvider<TreeElement>"
      pattern: "implements.*TreeDataProvider"
    - from: "claudeos-sessions/src/sidebar/session-item.ts"
      to: "vscode.TreeItem"
      via: "creates TreeItem with iconPath, contextValue, command, label highlights"
      pattern: "new.*TreeItem"
---

<objective>
Build the sessions sidebar TreeView with status-grouped sections, status icons, read/unread styling, notification badges, and all visual indicators specified in the user decisions.

Purpose: This is the primary sessions UI -- the sidebar tree that users see and interact with. It implements all SES-* visual and grouping requirements.

Output: SessionTreeProvider, TreeItem factories for sessions and groups, package.json contributes section with all commands, menus, and keybindings.
</objective>

<execution_context>
@/Users/bennett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bennett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-session-management/02-CONTEXT.md
@.planning/phases/02-session-management/02-RESEARCH.md
@.planning/phases/02-session-management/02-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 01 that this plan consumes. -->

From claudeos-sessions/src/supervisor/types.ts:
```typescript
export type SessionStatus = "active" | "idle" | "waiting" | "stopped" | "archived" | "zombie";

export interface Session {
  id: string;
  name: string;
  status: SessionStatus;
  createdAt: string;
  workdir?: string;
  model?: string;
  flags?: string[];
  pid?: number;
}
```

From claudeos-sessions/src/state/session-store.ts:
```typescript
export class SessionStore {
  getSessionsByStatus(status: SessionStatus): Session[];
  isUnread(id: string): boolean;
  getWaitingCount(): number;
  onDidChange: vscode.Event<void>;
  // other methods...
}
```

<!-- VS Code API patterns from research -->

TreeView.badge (ViewBadge):
```typescript
// badge is on TreeView (container level), NOT on TreeItem
interface ViewBadge { tooltip: string; value: number; }
treeView.badge = { value: waitingCount, tooltip: `${waitingCount} sessions waiting` };
```

TreeItemLabel with highlights:
```typescript
// highlights array makes text render with emphasis (bold-like)
const label: vscode.TreeItemLabel = {
  label: session.name,
  highlights: isUnread ? [[0, session.name.length]] : undefined,
};
```

Context value for menu filtering:
```typescript
// contextValue format: "session.{status}" for regex matching in when clauses
item.contextValue = `session.${session.status}`;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TreeItem factories for sessions and status groups</name>
  <files>
    claudeos-sessions/src/sidebar/session-item.ts,
    claudeos-sessions/src/sidebar/group-item.ts,
    claudeos-sessions/test/sidebar/session-item.test.ts
  </files>
  <behavior>
    - STATUS_ICONS maps each SessionStatus to the correct ThemeIcon: active->sync~spin (green), idle->debug-pause (yellow), waiting->question (orange), stopped->stop-circle (red), archived->archive (descriptionForeground), zombie->bug (errorForeground)
    - createSessionItem(session, isUnread) returns TreeItem with:
      - label as TreeItemLabel with highlights for unread, no highlights for read
      - iconPath set to STATUS_ICONS[session.status]
      - contextValue set to "session.{status}"
      - collapsibleState None
      - command set to claudeos.sessions.openTerminal with session argument
      - description shows "archived {timeAgo}" for archived, "{timeAgo}" for zombie
    - createGroupItem(status, sessions) returns TreeItem with:
      - label as status group name (capitalized: "Active", "Idle", etc.)
      - collapsibleState Expanded for active/idle/waiting/stopped, Collapsed for archived/zombie
      - description shows item count (e.g., "(3)")
      - contextValue "group.{status}"
      - No command (groups are not clickable)
    - createGroupItem returns null/skip for empty groups (except archived/zombie which always show if non-empty)
    - timeAgo helper: formats relative time (e.g., "3m ago", "2h ago", "1d ago")
  </behavior>
  <action>
    1. Create src/sidebar/session-item.ts:
       - Export STATUS_ICONS: Record<SessionStatus, vscode.ThemeIcon> mapping per user decision:
         - active: ThemeIcon('sync~spin', ThemeColor('charts.green'))
         - idle: ThemeIcon('debug-pause', ThemeColor('charts.yellow'))
         - waiting: ThemeIcon('question', ThemeColor('charts.orange'))
         - stopped: ThemeIcon('stop-circle', ThemeColor('charts.red'))
         - archived: ThemeIcon('archive', ThemeColor('descriptionForeground'))
         - zombie: ThemeIcon('bug', ThemeColor('errorForeground'))
       - Export createSessionItem(session: Session, isUnread: boolean): vscode.TreeItem
         - Use TreeItemLabel with highlights for unread per research
         - Set item.command to { command: 'claudeos.sessions.openTerminal', title: 'Open Terminal', arguments: [session] } -- per user decision, single-click opens terminal
         - Set contextValue to `session.${session.status}` for menu filtering
         - For archived sessions: description = `archived ${timeAgo(session.createdAt)}` (use createdAt as proxy until archivedAt available)
         - For zombie sessions: description = timeAgo(session.createdAt) (e.g., "3h ago")
       - Export timeAgo(dateStr: string): string helper

    2. Create src/sidebar/group-item.ts:
       - Export createGroupItem(status: SessionStatus, count: number): vscode.TreeItem
         - Display name: capitalize first letter of status
         - Description: `(${count})` to show item count per user decision (badge on collapsed headers)
         - CollapsibleState: Expanded for active/idle/waiting/stopped; Collapsed for archived/zombie per user decision
         - contextValue: `group.${status}`

    3. Write test/sidebar/session-item.test.ts:
       - Test STATUS_ICONS has entry for each SessionStatus
       - Test createSessionItem sets correct icon for each status
       - Test unread session has highlights in label
       - Test read session has no highlights
       - Test contextValue matches "session.{status}" pattern
       - Test command is claudeos.sessions.openTerminal
       - Test archived session shows "archived" in description
       - Test zombie session shows time-ago in description
       - Test createGroupItem sets correct collapsibleState (Expanded vs Collapsed)
       - Test createGroupItem description shows count
  </action>
  <verify>
    <automated>cd claudeos-sessions && npx vitest run test/sidebar/session-item.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - STATUS_ICONS maps all 6 statuses to correct codicons with correct ThemeColors
    - createSessionItem produces correctly configured TreeItems with status-aware icons, labels, commands, and contextValues
    - createGroupItem produces status group headers with correct expand/collapse state
    - All TreeItem factory tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SessionTreeProvider and package.json contributes</name>
  <files>
    claudeos-sessions/src/sidebar/session-tree.ts,
    claudeos-sessions/test/sidebar/session-tree.test.ts,
    claudeos-sessions/package.json
  </files>
  <behavior>
    - SessionTreeProvider implements TreeDataProvider<TreeElement>
    - getChildren(undefined) returns group items for statuses that have sessions (Active, Idle, Waiting, Stopped always if non-empty; Archived, Zombie always if non-empty)
    - getChildren(groupElement) returns session items for that status group, sorted most-recent-first
    - getTreeItem returns the TreeItem for any element
    - refresh() fires onDidChangeTreeData event
    - SessionTreeProvider.setTreeView(treeView) stores reference to update badge
    - Badge updates to show waiting count whenever refresh is called
    - SessionStore.onDidChange triggers refresh()
    - package.json contributes section has all viewsContainers, views, viewsWelcome, commands, menus, keybindings per research code examples
  </behavior>
  <action>
    1. Create src/sidebar/session-tree.ts:
       - Define TreeElement type: { type: 'group', status: SessionStatus } | { type: 'session', session: Session }
       - Export SessionTreeProvider class implementing vscode.TreeDataProvider<TreeElement>:
         - Constructor takes SessionStore
         - _onDidChangeTreeData = new vscode.EventEmitter<TreeElement | undefined | void>()
         - onDidChangeTreeData = _onDidChangeTreeData.event
         - treeView: vscode.TreeView<TreeElement> | undefined (set later via setTreeView)
         - getChildren(element?):
           - Root (no element): build group items. Define status order: ['active', 'idle', 'waiting', 'stopped', 'archived', 'zombie']. For each status, get sessions from store. Only include group if sessions.length > 0. Return array of group TreeElements.
           - Group element: get sessions for that status from store (already sorted most-recent-first by store). Return session TreeElements.
         - getTreeItem(element):
           - If group: call createGroupItem(status, count)
           - If session: call createSessionItem(session, store.isUnread(session.id))
         - refresh(): fire _onDidChangeTreeData.fire(undefined), update badge on treeView if set
         - setTreeView(tv): store reference, update badge immediately
         - updateBadge(): set treeView.badge = { value: store.getWaitingCount(), tooltip: "sessions waiting for input" } if count > 0, else treeView.badge = undefined
         - Wire store.onDidChange to refresh in constructor
         - dispose(): dispose EventEmitter

    2. Update package.json contributes section with the full manifest from research:
       - viewsContainers.activitybar: claudeos-sessions container with $(terminal-bash) icon
       - views.claudeos-sessions: single view "claudeos.sessions" named "Sessions"
       - viewsWelcome: empty state message with "Create Session" command link
       - commands: create, rename, openTerminal, stop, kill, archive, delete, revive, refresh -- each with correct title, icon, and category "ClaudeOS"
       - menus.view/title: create (+ icon) and refresh in navigation group
       - menus.view/item/context: all 7 context menu commands with correct when clauses using viewItem regex matching per research Pattern 4. Use separators via numbered groups (0_open, 1_edit, 2_lifecycle, 3_manage, 4_danger)
       - keybindings: F2 for rename when focusedView == claudeos.sessions
       - Also add activationEvents: ["onView:claudeos.sessions"]

    3. Write test/sidebar/session-tree.test.ts:
       - Mock SessionStore with test data (mix of active, idle, waiting, archived, zombie sessions)
       - Test getChildren(undefined) returns correct group elements in status order
       - Test getChildren(undefined) omits empty groups
       - Test getChildren(groupElement) returns sessions sorted most-recent-first
       - Test getTreeItem for group returns correct collapsibleState and description
       - Test getTreeItem for session returns correct icon and contextValue
       - Test refresh fires onDidChangeTreeData
       - Test badge updates to waiting count
       - Test onDidChange from store triggers refresh
  </action>
  <verify>
    <automated>cd claudeos-sessions && npx vitest run test/sidebar/ --reporter=verbose</automated>
  </verify>
  <done>
    - SessionTreeProvider shows sessions grouped by status with correct ordering
    - Empty groups are hidden; archived/zombie groups collapsed by default
    - TreeView badge shows waiting session count
    - package.json has complete contributes section with all commands, menus, keybindings
    - All sidebar tests pass
  </done>
</task>

</tasks>

<verification>
- `cd claudeos-sessions && npx vitest run test/sidebar/ --reporter=verbose` -- all sidebar tests pass
- `cd claudeos-sessions && npx tsc --noEmit` -- TypeScript compiles cleanly
- Inspect package.json contributes: verify all 9 commands registered, context menu has correct when clauses, F2 keybinding present
</verification>

<success_criteria>
- Sidebar TreeView groups sessions by status (Active, Idle, Waiting, Stopped expanded; Archived, Zombie collapsed)
- Each session shows correct status codicon with correct ThemeColor
- Unread sessions show bold (highlighted) labels; read sessions show normal labels
- Zombie sessions show bug icon with error color and time-ago description
- Archived sessions show archive icon with archived-date description
- TreeView badge shows total waiting session count
- Sessions sorted most-recent-first within each group
- Context menu items filtered correctly by session status via contextValue
- F2 keybinding triggers rename command
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-management/02-02-SUMMARY.md`
</output>
