---
phase: 08-operational-polish-tech-debt
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified: [flake.nix]
autonomous: false
gap_closure: true
requirements: [SUP-07, SUP-08, DEP-02, IMP-03]

must_haves:
  truths:
    - "flake.nix npmDepsHash contains a real sha256 hash, not the placeholder sha256-AAAA value"
    - "nix build .#default does not fail with hash mismatch on npmDepsHash"
  artifacts:
    - path: "flake.nix"
      provides: "Real npmDepsHash for supervisor buildNpmPackage derivation"
      contains: 'npmDepsHash = "sha256-'
  key_links:
    - from: "flake.nix"
      to: "supervisor/package-lock.json"
      via: "npmDepsHash integrity check"
      pattern: "npmDepsHash"
---

<objective>
Replace the placeholder npmDepsHash in flake.nix with the real content hash from a Nix build attempt.

Purpose: The placeholder sha256-AAAA value causes `nix build` to fail with a hash mismatch. Without the real hash, the container image cannot be built via Nix. This is the sole remaining gap from Phase 8 verification.

Output: flake.nix line 103 updated with the real sha256 hash.
</objective>

<execution_context>
@/Users/bennett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bennett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-operational-polish-tech-debt/08-01-SUMMARY.md
@.planning/phases/08-operational-polish-tech-debt/08-VERIFICATION.md

@flake.nix
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Compute real npmDepsHash via nix build</name>
  <action>
    This task requires Nix to be installed on the machine. The previous plan execution confirmed Nix was not available in the Claude execution environment.

    The user must run the following command on a machine with Nix installed:

    ```bash
    nix build .#default 2>&1 | grep "got:"
    ```

    This will output a line like:
    ```
    got:    sha256-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX=
    ```

    Provide the full sha256-... hash value (including the sha256- prefix and trailing =).
  </action>
  <resume-signal>Paste the sha256 hash from the nix build output (e.g., sha256-abc123...=)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder npmDepsHash in flake.nix</name>
  <files>flake.nix</files>
  <action>
    Replace the placeholder hash on line 103 of flake.nix:

    Current: `npmDepsHash = "sha256-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=";`

    Replace with: `npmDepsHash = "{hash_from_task_1}";`

    Use the exact hash value provided by the user in Task 1. Do not modify any other lines in flake.nix.

    Additionally, address the verification warning about the extensionVsix derivation: on line 68, `npm ci --ignore-scripts 2>/dev/null || true` silently swallows failures in the Nix sandbox. Add a comment above line 68 making the limitation explicit, or replace `|| true` with `|| echo "WARN: npm ci failed -- VSIX may be incomplete"` so failures are at least visible in build logs rather than silently ignored.
  </action>
  <verify>
    <automated>grep -n "npmDepsHash" flake.nix | grep -v "AAAA"</automated>
  </verify>
  <done>
    - flake.nix line 103 contains a real sha256 hash (not the AAAA placeholder)
    - extensionVsix npm ci failure is no longer silently swallowed
  </done>
</task>

</tasks>

<verification>
- `grep "sha256-AAAA" flake.nix` returns no matches (placeholder removed)
- `grep "npmDepsHash" flake.nix` shows a real hash value
- Optionally: `nix build .#default` completes without hash mismatch error
</verification>

<success_criteria>
flake.nix npmDepsHash contains a real sha256 hash derived from `nix build` output. The placeholder sha256-AAAA value is gone. Phase 8 success criterion 3 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-operational-polish-tech-debt/08-02-SUMMARY.md`
</output>
