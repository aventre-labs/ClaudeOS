---
phase: 14-theme-foundation-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - claudeos-secrets/src/webview/secrets-panel.ts
autonomous: true
requirements:
  - THEME-03
  - THEME-04

must_haves:
  truths:
    - "Secrets panel CSS has zero hardcoded hex color values (no fallbacks in var() calls)"
    - "Changing VS Code theme automatically updates all Secrets panel colors"
    - "THEME-03 is acknowledged as superseded by CONTEXT.md decision (wizard keeps independent theme.css)"
  artifacts:
    - path: "claudeos-secrets/src/webview/secrets-panel.ts"
      provides: "Secrets panel with pure var(--vscode-*) CSS, no custom --claudeos-* variables"
      contains: "var(--vscode-"
      min_lines: 400
  key_links:
    - from: "claudeos-secrets/src/webview/secrets-panel.ts"
      to: "config/settings.json"
      via: "VS Code injects workbench.colorCustomizations values as --vscode-* CSS variables in webviews"
      pattern: "var\\(--vscode-"
---

<objective>
Migrate the Secrets panel CSS to pure var(--vscode-*) variables, removing all --claudeos-* custom properties and hardcoded fallbacks.

Purpose: Ensures the Secrets panel respects the active VS Code theme, completing the THEME-04 requirement that changing themes updates all custom panels. Also addresses THEME-03 by documenting the wizard exemption.
Output: Updated secrets-panel.ts with fully theme-compliant CSS.

**THEME-03 note:** REQUIREMENTS.md states "Setup wizard CSS uses var(--vscode-*) variables instead of its own theme.css palette." This is SUPERSEDED by the CONTEXT.md locked decision: "Wizard keeps its own independent theme.css -- it runs before code-server starts and has no VS Code theme context." The wizard genuinely cannot use VS Code variables. This plan marks THEME-03 as satisfied by acknowledgment of the context decision.
</objective>

<execution_context>
@/Users/bennett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bennett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-theme-foundation-infrastructure/14-CONTEXT.md
@.planning/phases/14-theme-foundation-infrastructure/14-RESEARCH.md
@.planning/phases/14-theme-foundation-infrastructure/14-UI-SPEC.md
@.planning/phases/14-theme-foundation-infrastructure/14-01-SUMMARY.md

<interfaces>
<!-- Secrets panel TypeScript class structure -->
From claudeos-secrets/src/webview/secrets-panel.ts:
```typescript
export class SecretsPanel {
  static currentPanel: SecretsPanel | undefined;
  static createOrShow(context, client, treeProvider, onSecretChange?, secretName?): void;
  static postNewSecret(): void;
  _getHtmlForWebview(webview: vscode.Webview): string;  // CSS lives here
  _handleMessage(message): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Secrets panel CSS to pure var(--vscode-*) variables</name>
  <files>claudeos-secrets/src/webview/secrets-panel.ts</files>
  <read_first>
    - claudeos-secrets/src/webview/secrets-panel.ts (full file -- understand all CSS in the _getHtmlForWebview template literal)
    - .planning/phases/14-theme-foundation-infrastructure/14-UI-SPEC.md (CSS Migration Rules section, Variable Mapping Reference table, Secrets Panel copywriting, error states, destructive actions)
  </read_first>
  <action>
Modify ONLY the CSS within the `_getHtmlForWebview` method's template literal. The TypeScript class structure, _handleMessage, createOrShow, postNewSecret, constructor, and all JavaScript in the script tag remain UNCHANGED.

**CSS changes:**

1. Remove the `:root` block:
```css
/* DELETE this entire block */
:root {
  --claudeos-accent: #c084fc;
  --claudeos-accent-hover: #a855f7;
}
```

2. Apply ALL variable migrations from the UI-SPEC Variable Mapping Reference table. Every occurrence must be changed:

| Old Pattern | New Pattern | Occurrences |
|-------------|-------------|-------------|
| `var(--vscode-panel-border, #333)` | `var(--vscode-panel-border)` | 4 occurrences (secrets-list border-right, header border-bottom, toast border, value-row btn borders) |
| `var(--vscode-sideBar-background, var(--vscode-editor-background))` | `var(--vscode-sideBar-background)` | 1 occurrence (secrets-list background) |
| `var(--vscode-list-hoverBackground, rgba(255,255,255,0.04))` | `var(--vscode-list-hoverBackground)` | 2 occurrences (add-btn hover, secret-item hover) |
| `var(--vscode-descriptionForeground, #888)` | `var(--vscode-descriptionForeground)` | 3 occurrences (category, list-empty, empty-state) |
| `var(--vscode-list-activeSelectionBackground, rgba(255,255,255,0.08))` | `var(--vscode-list-activeSelectionBackground)` | 1 occurrence (secret-item.active) |
| `var(--vscode-input-background, #1e1e1e)` | `var(--vscode-input-background)` | 1 occurrence (form inputs) |
| `var(--vscode-input-foreground, #ccc)` | `var(--vscode-input-foreground)` | 1 occurrence (form inputs) |
| `var(--vscode-input-border, #444)` | `var(--vscode-input-border)` | 3 occurrences (form inputs, value-row btns, btn-cancel) |
| `var(--vscode-button-secondaryBackground, #333)` | `var(--vscode-button-secondaryBackground)` | 2 occurrences (toggle-btn, btn-cancel) |
| `var(--vscode-button-secondaryHoverBackground, #444)` | `var(--vscode-button-secondaryHoverBackground)` | 2 occurrences (toggle-btn hover, btn-cancel hover) |
| `var(--vscode-button-secondaryForeground, #ccc)` | `var(--vscode-button-secondaryForeground)` | 1 occurrence (btn-cancel) |
| `var(--vscode-notificationsBackground, #252525)` | `var(--vscode-notificationsBackground)` | 1 occurrence (toast) |
| `var(--vscode-notificationsForeground, #ccc)` | `var(--vscode-notificationsForeground)` | 1 occurrence (toast) |
| `var(--vscode-inputValidation-errorBackground, #5a1d1d)` | `var(--vscode-inputValidation-errorBackground)` | 1 occurrence (btn-delete) |
| `var(--vscode-errorForeground, #f44)` | `var(--vscode-errorForeground)` | 1 occurrence (btn-delete) |
| `var(--vscode-inputValidation-errorBorder, #be1100)` | `var(--vscode-inputValidation-errorBorder)` | 1 occurrence (btn-delete) |
| `var(--vscode-inputValidation-warningBackground, ...)` | `var(--vscode-inputValidation-warningBackground)` | if present |
| `var(--claudeos-accent)` | `var(--vscode-focusBorder)` | 3 occurrences (add-btn hover color, secret-item.active border-left-color, form input:focus border-color) |
| `var(--claudeos-accent-hover)` | `var(--vscode-button-hoverBackground)` | if present |

3. Fix the save button (`.btn-save`):
```css
/* BEFORE */
.btn-save {
  background: var(--claudeos-accent);
  color: #fff;
  ...
}
.btn-save:hover {
  background: var(--claudeos-accent-hover);
}

/* AFTER */
.btn-save {
  background: var(--vscode-button-background);
  color: var(--vscode-button-foreground);
  border: none;
  padding: 8px 20px;
  border-radius: 4px;
  cursor: pointer;
  font-weight: 500;
}
.btn-save:hover {
  background: var(--vscode-button-hoverBackground);
}
```

4. Do NOT add noise texture or radial glow to the Secrets panel. Per UI-SPEC: "Secrets | No | No | Utility panel -- clean, distraction-free".

5. Do NOT modify any HTML structure or JavaScript behavior. Only CSS changes.

**After all replacements, verify there are ZERO instances of:**
- `--claudeos-` (any custom variable)
- `, #` inside a `var()` call (hex fallback)
- `, rgba(` inside a `var()` call (rgba fallback)
  </action>
  <verify>
    <automated>cd /Users/bennett/Desktop/Projects/ClaudeOS && ! grep -q "\-\-claudeos-" claudeos-secrets/src/webview/secrets-panel.ts && ! grep -qP "var\(--vscode-[^,)]+, #" claudeos-secrets/src/webview/secrets-panel.ts && ! grep -qP "var\(--vscode-[^,)]+, rgba" claudeos-secrets/src/webview/secrets-panel.ts && grep -c "var(--vscode-" claudeos-secrets/src/webview/secrets-panel.ts | xargs -I{} test {} -gt 15 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - claudeos-secrets/src/webview/secrets-panel.ts does NOT contain `--claudeos-accent`
    - claudeos-secrets/src/webview/secrets-panel.ts does NOT contain `--claudeos-accent-hover`
    - claudeos-secrets/src/webview/secrets-panel.ts does NOT contain any `:root` block with custom variables
    - claudeos-secrets/src/webview/secrets-panel.ts contains `var(--vscode-` at least 15 times
    - claudeos-secrets/src/webview/secrets-panel.ts does NOT contain fallback pattern `, #333)` or `, #888)` or `, #1e1e1e)` or `, #ccc)` or `, #444)` or `, #252525)` etc. inside var() calls
    - claudeos-secrets/src/webview/secrets-panel.ts `.btn-save` uses `var(--vscode-button-background)` not `var(--claudeos-accent)`
    - claudeos-secrets/src/webview/secrets-panel.ts `.btn-save` uses `var(--vscode-button-foreground)` not `#fff`
    - claudeos-secrets/src/webview/secrets-panel.ts `.secret-item.active` uses `var(--vscode-focusBorder)` for border-left-color
    - claudeos-secrets/src/webview/secrets-panel.ts form input:focus uses `var(--vscode-focusBorder)` for border-color
    - claudeos-secrets/src/webview/secrets-panel.ts does NOT contain `feTurbulence` or `radial-gradient` (no effects on Secrets panel)
    - `cd claudeos-secrets && npx tsc --noEmit` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>Secrets panel CSS uses pure var(--vscode-*) variables with no custom --claudeos-* properties and no hardcoded hex fallbacks in var() calls; changing VS Code theme automatically updates all Secrets panel colors</done>
</task>

</tasks>

<verification>
- `grep -c "var(--vscode-" claudeos-secrets/src/webview/secrets-panel.ts` shows 15+ occurrences
- `grep -E "\-\-claudeos-" claudeos-secrets/src/webview/secrets-panel.ts` shows 0 results
- `grep -P "var\(--vscode-[^,)]+, #" claudeos-secrets/src/webview/secrets-panel.ts` shows 0 results
- `cd claudeos-secrets && npx tsc --noEmit` passes
</verification>

<success_criteria>
The Secrets panel CSS uses only var(--vscode-*) variables -- no custom --claudeos-* properties, no hardcoded hex color fallbacks in var() calls. The save button uses var(--vscode-button-background/foreground), hover states use var(--vscode-focusBorder), and all other inputs/borders/backgrounds reference standard VS Code theme variables. THEME-03 is addressed by acknowledging the CONTEXT.md decision that the wizard keeps its own independent theme.css (the wizard runs before code-server and has no VS Code context). THEME-04 is satisfied because all webview panels (Home + Secrets) now use pure theme variables that automatically update when the theme changes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-theme-foundation-infrastructure/14-04-SUMMARY.md`
</output>
