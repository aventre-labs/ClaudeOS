---
phase: 14-theme-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.json
autonomous: true
requirements:
  - THEME-01
  - THEME-05

must_haves:
  truths:
    - "ClaudeOS uses a custom dark color theme with gold accent #d4a054 by default"
    - "Copilot AI features are fully disabled -- no chat sidebar, no inline suggestions"
  artifacts:
    - path: "config/settings.json"
      provides: "Complete ClaudeOS dark theme via workbench.colorCustomizations + Copilot disable settings"
      contains: "workbench.colorCustomizations"
  key_links:
    - from: "config/settings.json"
      to: "VS Code runtime"
      via: "settings.json is copied to /app/settings.json by Dockerfile and flake.nix, code-server reads it as default settings"
      pattern: "workbench\\.colorCustomizations"
---

<objective>
Define the ClaudeOS Dark theme palette and disable Copilot AI features in config/settings.json.

Purpose: Establishes the visual foundation that all webview panels will reference through `var(--vscode-*)` CSS variables. Also removes Copilot UI clutter.
Output: Updated config/settings.json with complete colorCustomizations overlay and Copilot disable flags.
</objective>

<execution_context>
@/Users/bennett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bennett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-theme-foundation-infrastructure/14-CONTEXT.md
@.planning/phases/14-theme-foundation-infrastructure/14-RESEARCH.md
@.planning/phases/14-theme-foundation-infrastructure/14-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ClaudeOS Dark theme and Copilot disable to settings.json</name>
  <files>config/settings.json</files>
  <read_first>
    - config/settings.json (current state -- preserve all existing non-theme settings)
    - .planning/phases/14-theme-foundation-infrastructure/14-UI-SPEC.md (authoritative color values in "Complete workbench.colorCustomizations Reference" table)
    - .planning/phases/14-theme-foundation-infrastructure/14-RESEARCH.md (Copilot disable settings)
  </read_first>
  <action>
Update config/settings.json to add the ClaudeOS Dark theme and Copilot disable settings. Preserve ALL existing settings (editor.fontSize, editor.fontFamily, editor.tabSize, editor.minimap.enabled, editor.wordWrap, editor.formatOnSave, terminal.integrated.fontSize, terminal.integrated.defaultProfile.linux, terminal.integrated.scrollback, telemetry.telemetryLevel, workbench.startupEditor, extensions.autoUpdate, extensions.autoCheckUpdates, update.mode).

The existing `"workbench.colorTheme": "Default Dark Modern"` stays as-is (the ClaudeOS theme is an overlay on Default Dark Modern via colorCustomizations).

Add `"workbench.colorCustomizations"` with these EXACT values from the UI-SPEC:

```json
"workbench.colorCustomizations": {
  "editor.background": "#0e0e10",
  "editor.foreground": "#b0a99e",
  "editor.lineHighlightBackground": "#131315",
  "editor.selectionBackground": "rgba(212, 160, 84, 0.20)",
  "editorCursor.foreground": "#d4a054",
  "activityBar.background": "#0a0a0c",
  "activityBar.foreground": "#d4a054",
  "activityBar.inactiveForeground": "#6b6560",
  "activityBarBadge.background": "#d4a054",
  "activityBarBadge.foreground": "#0a0a0c",
  "sideBar.background": "#0c0c0e",
  "sideBar.foreground": "#b0a99e",
  "sideBar.border": "rgba(255, 255, 255, 0.06)",
  "sideBarTitle.foreground": "#ede8e0",
  "titleBar.activeBackground": "#0a0a0c",
  "titleBar.activeForeground": "#ede8e0",
  "titleBar.inactiveBackground": "#0a0a0c",
  "statusBar.background": "#0a0a0c",
  "statusBar.foreground": "#8a847c",
  "tab.activeBackground": "#131315",
  "tab.inactiveBackground": "#0a0a0c",
  "tab.activeBorderTop": "#d4a054",
  "tab.border": "rgba(255, 255, 255, 0.06)",
  "panel.background": "#0a0a0c",
  "panel.border": "rgba(255, 255, 255, 0.06)",
  "button.background": "#d4a054",
  "button.foreground": "#0a0a0c",
  "button.hoverBackground": "#e0b56a",
  "input.background": "rgba(255, 255, 255, 0.03)",
  "input.border": "rgba(255, 255, 255, 0.08)",
  "input.foreground": "#b0a99e",
  "input.placeholderForeground": "#6b6560",
  "focusBorder": "#d4a054",
  "list.activeSelectionBackground": "rgba(212, 160, 84, 0.15)",
  "list.hoverBackground": "rgba(255, 255, 255, 0.04)",
  "list.activeSelectionForeground": "#ede8e0",
  "notifications.background": "#131315",
  "notifications.foreground": "#b0a99e"
}
```

Add Copilot disable settings:
```json
"chat.disableAIFeatures": true,
"github.copilot.enable": {
  "*": false
}
```

The final file should be valid JSON with all original settings plus the three new blocks.
  </action>
  <verify>
    <automated>cat config/settings.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert 'workbench.colorCustomizations' in d; assert d['workbench.colorCustomizations']['editor.background']=='#0e0e10'; assert d['workbench.colorCustomizations']['focusBorder']=='#d4a054'; assert d['chat.disableAIFeatures']==True; assert d['github.copilot.enable']=={'*':False}; assert d['workbench.colorTheme']=='Default Dark Modern'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - config/settings.json is valid JSON (python3 -c "import json; json.load(open('config/settings.json'))" exits 0)
    - config/settings.json contains key "workbench.colorCustomizations" with at least 30 color tokens
    - config/settings.json contains "editor.background": "#0e0e10"
    - config/settings.json contains "activityBar.foreground": "#d4a054"
    - config/settings.json contains "focusBorder": "#d4a054"
    - config/settings.json contains "button.background": "#d4a054"
    - config/settings.json contains "chat.disableAIFeatures": true
    - config/settings.json contains "github.copilot.enable" with {"*": false}
    - config/settings.json preserves "workbench.colorTheme": "Default Dark Modern"
    - config/settings.json preserves "editor.fontSize": 14
    - config/settings.json preserves "terminal.integrated.defaultProfile.linux": "tmux"
  </acceptance_criteria>
  <done>settings.json contains the full ClaudeOS Dark theme via workbench.colorCustomizations (35 color tokens with gold #d4a054 accent), Copilot is disabled, and all pre-existing settings are preserved</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; d=json.load(open('config/settings.json')); print(len(d['workbench.colorCustomizations']), 'color tokens')"` shows 35 tokens
- `grep -c "d4a054" config/settings.json` shows at least 6 occurrences (activity bar, badge, cursor, tab border, buttons, focus)
- `grep "disableAIFeatures" config/settings.json` shows the setting
</verification>

<success_criteria>
config/settings.json defines the complete ClaudeOS Dark theme as a colorCustomizations overlay on Default Dark Modern, with gold #d4a054 accent color on 6+ UI elements, and Copilot AI features are disabled via chat.disableAIFeatures and github.copilot.enable settings.
</success_criteria>

<output>
After completion, create `.planning/phases/14-theme-foundation-infrastructure/14-01-SUMMARY.md`
</output>
