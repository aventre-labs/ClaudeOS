---
phase: 12-wizard-ui-and-build-progress
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - supervisor/wizard/src/App.tsx
  - supervisor/wizard/src/App.module.css
  - supervisor/wizard/src/components/Stepper.tsx
  - supervisor/wizard/src/components/Stepper.module.css
  - supervisor/wizard/src/components/RailwayStep.tsx
  - supervisor/wizard/src/components/RailwayStep.module.css
  - supervisor/wizard/src/components/AnthropicStep.tsx
  - supervisor/wizard/src/components/AnthropicStep.module.css
  - supervisor/wizard/src/components/LaunchStep.tsx
  - supervisor/wizard/src/components/LaunchStep.module.css
  - supervisor/wizard/src/components/BuildProgress.tsx
  - supervisor/wizard/src/components/BuildProgress.module.css
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "User sees a horizontal stepper showing Railway, Claude, Launch steps with active/completed states"
    - "User sees build progress footer with extension name and count while extensions install"
    - "Completed auth steps show green checkmark status with Sign Out link"
    - "Alternative auth methods are always visible below primary method, not hidden"
    - "Railway step shows large pairing code with copy button when in progress"
    - "Anthropic step shows API key input and claude login as equal side-by-side options"
    - "Build progress footer shows error with retry button on failure"
    - "Build progress footer fades after completion"
  artifacts:
    - path: "supervisor/wizard/src/App.tsx"
      provides: "Top-level state machine wiring SSE events to UI state via wizardReducer"
      min_lines: 60
    - path: "supervisor/wizard/src/components/Stepper.tsx"
      provides: "Horizontal step indicator bar"
      exports: ["Stepper"]
    - path: "supervisor/wizard/src/components/RailwayStep.tsx"
      provides: "Railway auth step with pairing code, token input, completed state"
      exports: ["RailwayStep"]
    - path: "supervisor/wizard/src/components/AnthropicStep.tsx"
      provides: "Anthropic auth step with API key and claude login options"
      exports: ["AnthropicStep"]
    - path: "supervisor/wizard/src/components/LaunchStep.tsx"
      provides: "Final launch step"
      exports: ["LaunchStep"]
    - path: "supervisor/wizard/src/components/BuildProgress.tsx"
      provides: "Footer bar with progress bar, extension name, completion/error states"
      exports: ["BuildProgress"]
  key_links:
    - from: "supervisor/wizard/src/App.tsx"
      to: "supervisor/wizard/src/hooks/useSSE.ts"
      via: "useSSE hook consuming /api/v1/wizard/events"
      pattern: "useSSE"
    - from: "supervisor/wizard/src/App.tsx"
      to: "supervisor/wizard/src/types.ts"
      via: "useReducer with wizardReducer"
      pattern: "wizardReducer"
    - from: "supervisor/wizard/src/components/RailwayStep.tsx"
      to: "supervisor/wizard/src/api/wizard.ts"
      via: "startRailwayLogin API call"
      pattern: "startRailwayLogin"
    - from: "supervisor/wizard/src/components/AnthropicStep.tsx"
      to: "supervisor/wizard/src/api/wizard.ts"
      via: "submitAnthropicKey and startClaudeLogin API calls"
      pattern: "submitAnthropicKey|startClaudeLogin"
---

<objective>
Build all wizard UI components: horizontal stepper, Railway auth step, Anthropic auth step, Launch step, and build progress footer. Wire everything together in App.tsx with the state machine (wizardReducer) and SSE hook from Plan 01.

Purpose: This is the core user-facing work of Phase 12. Users see a polished multi-step wizard instead of a blank page during first boot.
Output: Complete, styled wizard UI that responds to SSE events and drives auth flows through the backend API.
</objective>

<execution_context>
@/Users/bennett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bennett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wizard-ui-and-build-progress/12-CONTEXT.md
@.planning/phases/12-wizard-ui-and-build-progress/12-RESEARCH.md
@.planning/phases/12-wizard-ui-and-build-progress/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — types, API client, hooks the UI components consume -->

From supervisor/wizard/src/types.ts (created in Plan 01):
```typescript
type WizardStep = 'railway' | 'anthropic' | 'launch';

interface WizardUIState {
  activeStep: WizardStep;
  railway: { status: 'idle' | 'pairing' | 'complete' | 'error'; pairingCode?: string; url?: string; error?: string };
  anthropic: { status: 'idle' | 'validating' | 'loginStarted' | 'complete' | 'error'; loginUrl?: string; error?: string };
  build: { status: 'idle' | 'installing' | 'complete' | 'error'; current?: string; progress?: number; total?: number; error?: string };
}

type WizardAction = /* discriminated union — see Plan 01 */;
function wizardReducer(state: WizardUIState, action: WizardAction): WizardUIState;
```

From supervisor/wizard/src/api/wizard.ts (created in Plan 01):
```typescript
export function getWizardStatus(): Promise<WizardStatus>;
export function startRailwayLogin(): Promise<void>;
export function submitAnthropicKey(apiKey: string): Promise<void>;
export function startClaudeLogin(): Promise<void>;
export function completeWizard(): Promise<void>;
```

From supervisor/wizard/src/hooks/useSSE.ts (created in Plan 01):
```typescript
export function useSSE(options: { url: string; handlers: Record<string, (data: unknown) => void>; maxRetries?: number; retryDelay?: number }): { connected: boolean };
```

From first-boot/setup.html (color palette reference):
- Background: #1e1e1e
- Card background: #252526
- Border: #3c3c3c
- Accent/primary: #4a9eff
- Text: #cccccc
- Heading text: #ffffff
- Font: system font stack (-apple-system, BlinkMacSystemFont, "Segoe UI", ...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Stepper, auth step components, and BuildProgress with CSS modules</name>
  <files>
    supervisor/wizard/src/components/Stepper.tsx
    supervisor/wizard/src/components/Stepper.module.css
    supervisor/wizard/src/components/RailwayStep.tsx
    supervisor/wizard/src/components/RailwayStep.module.css
    supervisor/wizard/src/components/AnthropicStep.tsx
    supervisor/wizard/src/components/AnthropicStep.module.css
    supervisor/wizard/src/components/LaunchStep.tsx
    supervisor/wizard/src/components/LaunchStep.module.css
    supervisor/wizard/src/components/BuildProgress.tsx
    supervisor/wizard/src/components/BuildProgress.module.css
  </files>
  <action>
    All components use CSS Modules for scoped styling. Use the ClaudeOS dark theme palette from setup.html: bg #1e1e1e, card #252526, border #3c3c3c, accent #4a9eff, text #cccccc, white #ffffff. System font stack.

    1. **Stepper.tsx** — Horizontal top stepper bar per CONTEXT.md locked decisions:
       - Props: `steps: Array<{ key: string; label: string; completed: boolean; active: boolean }>`, `onStepClick: (key: string) => void`
       - Render horizontal row: each step has a circle indicator (number or checkmark if completed) and label below
       - Connector lines between steps, active when step is completed or active
       - Active step highlighted with accent color (#4a9eff), completed steps show checkmark with green (#4ec930)
       - Clicking a step calls onStepClick (for navigating between steps)

    2. **RailwayStep.tsx** — Railway auth with all interaction states from CONTEXT.md:
       - Props: `status: 'idle' | 'pairing' | 'complete' | 'error'`, `pairingCode?: string`, `url?: string`, `error?: string`, `onStart: () => void`, `onSignOut?: () => void`
       - **idle state:** "Sign in with Railway" button prominently displayed. Brief explanation: "Verifies you own this Railway project". Small "Use auth token instead" link below (AUTH-05). The auth token link is always visible, not hidden.
       - **pairing state:** Large pairing code displayed like a 2FA code (large monospace font, letter-spaced). Copy button next to code. Instructions: "Enter this code at railway.com/cli-login" with the URL as a clickable link opening in new tab. "Waiting for confirmation..." text with subtle spinner below.
       - **complete state (AUTH-04):** Green checkmark with "Railway: signed in" status text. Subtle "Sign Out" link. Step content collapsed but expandable.
       - **error state:** Error message text with "Try again" button. No automatic retry.

    3. **AnthropicStep.tsx** — Anthropic auth with dual methods per CONTEXT.md:
       - Props: `status: 'idle' | 'validating' | 'loginStarted' | 'complete' | 'error'`, `loginUrl?: string`, `error?: string`, `onSubmitKey: (key: string) => void`, `onStartLogin: () => void`, `onSignOut?: () => void`
       - **idle state:** Two auth methods shown as equal options side by side (AUTH-05 — not hidden behind expand):
         - Left: API key input (type="password") with "Validate Key" submit button. Label: "Anthropic API Key"
         - Right: "Sign in with Anthropic" button (starts claude login flow). Label: "Interactive Login"
         - Brief explanation above both: "Authenticate Claude Code with Anthropic"
       - **validating state:** API key input disabled, spinner on submit button
       - **loginStarted state:** Show login URL as clickable link. "Waiting for authentication..." with spinner.
       - **complete state (AUTH-04):** Green checkmark with "Anthropic: authenticated" and method used (API key or Interactive). "Sign Out" link.
       - **error state:** Error message with "Try again" button.

    4. **LaunchStep.tsx** — Final step:
       - Props: `railwayComplete: boolean`, `anthropicComplete: boolean`, `buildComplete: boolean`, `onLaunch: () => void`
       - Show checklist of prerequisites with green checkmarks for completed items: "Railway authenticated", "Anthropic authenticated", "Extensions installed"
       - "Launch ClaudeOS" button enabled only when all prerequisites are complete
       - Brief text: "All set! Click Launch to start your ClaudeOS instance."

    5. **BuildProgress.tsx** — Persistent footer bar per CONTEXT.md locked decisions:
       - Props: `status: 'idle' | 'installing' | 'complete' | 'error'`, `currentExtension?: string`, `progress?: number`, `total?: number`, `error?: string`, `onRetry?: () => void`
       - **idle state:** Don't render (return null)
       - **installing state:** Progress bar (thin horizontal bar) + text: "Installing {currentExtension}... ({progress}/{total})"
       - **complete state:** Green checkmark "Extensions installed", stays for 2 seconds then fades out (CSS transition on opacity)
       - **error state:** Error message text with "Retry" button. Auth steps remain accessible (this is just a footer).
       - Footer is fixed to bottom of the wizard card, visible during all auth steps
  </action>
  <verify>
    <automated>cd /Users/bennett/Desktop/Projects/ClaudeOS/supervisor/wizard && npx vite build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>
    - All 5 components (Stepper, RailwayStep, AnthropicStep, LaunchStep, BuildProgress) exist with CSS Modules
    - Each component handles all interaction states defined in CONTEXT.md
    - RailwayStep shows "Use auth token instead" link always visible (AUTH-05)
    - AnthropicStep shows API key and interactive login side by side (AUTH-05)
    - Completed states show green checkmark with Sign Out (AUTH-04)
    - BuildProgress shows progress bar with extension name and count (SETUP-01)
    - Vite build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx state machine with SSE events and render wizard layout</name>
  <files>
    supervisor/wizard/src/App.tsx
    supervisor/wizard/src/App.module.css
  </files>
  <action>
    Replace the placeholder App.tsx from Plan 01 with the full wizard application.

    1. **App.tsx** — Top-level component orchestrating the entire wizard:
       - Use `useReducer(wizardReducer, initialState)` for state management
       - Use `useWizardStatus()` hook to fetch initial wizard state on mount
       - On initial status loaded, dispatch `INIT` action to populate state from server (pre-completed steps show as complete)
       - Use `useSSE` hook connected to `/api/v1/wizard/events` with handlers that dispatch actions:
         - `railway:started` -> dispatch RAILWAY_STARTED with pairingCode and url
         - `railway:complete` -> dispatch RAILWAY_COMPLETE
         - `anthropic:key-validated` -> dispatch ANTHROPIC_KEY_VALIDATED
         - `anthropic:login-started` -> dispatch ANTHROPIC_LOGIN_STARTED
         - `anthropic:login-complete` -> dispatch ANTHROPIC_LOGIN_COMPLETE
         - `build:progress` -> dispatch BUILD_PROGRESS with current, progress, total
         - `build:complete` -> dispatch BUILD_COMPLETE
         - `build:error` -> dispatch BUILD_ERROR
         - `wizard:step-completed` -> dispatch STEP_COMPLETED (auto-advance to next incomplete step)
       - **CRITICAL:** Memoize the SSE handlers object with `useRef` to prevent reconnection on every render (Pitfall 5)
       - Derive `steps` array for Stepper from state (railway completed?, anthropic completed?, launch active?)
       - On step click: dispatch SET_ACTIVE_STEP to navigate between steps

    2. **Layout** (App.module.css) — per CONTEXT.md locked decisions:
       - Full-page dark background (#1e1e1e)
       - Centered card layout, max-width ~600px, with card background (#252526), border (#3c3c3c), border-radius
       - ClaudeOS logo + "ClaudeOS Setup" heading above the stepper (use text, not image — "Claude" in white, "OS" in accent #4a9eff)
       - Stepper bar at top of card
       - Active step content rendered below stepper (only active step visible)
       - BuildProgress footer at bottom of card, always visible when build is in progress
       - Loading state: show a simple spinner/skeleton while initial wizard status loads

    3. **Step navigation logic:**
       - Active step defaults to first incomplete step
       - When INIT dispatched with server state, set activeStep to first step where completed=false
       - When a step completes (STEP_COMPLETED), auto-advance to next incomplete step
       - User can click completed steps to review them (collapsed view with status)
       - User can click the active step indicator in stepper

    4. **API call wiring:**
       - RailwayStep.onStart calls `startRailwayLogin()` (catch errors, dispatch error action)
       - AnthropicStep.onSubmitKey calls `submitAnthropicKey(key)` (catch errors, dispatch error action)
       - AnthropicStep.onStartLogin calls `startClaudeLogin()` (catch errors, dispatch error action)
       - LaunchStep.onLaunch calls `completeWizard()` (catch errors, show error state)
  </action>
  <verify>
    <automated>cd /Users/bennett/Desktop/Projects/ClaudeOS/supervisor/wizard && npx vite build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>
    - App.tsx renders full wizard layout with centered card, logo, stepper, active step content, and build progress footer
    - SSE events drive state transitions via wizardReducer
    - Initial wizard status from server populates pre-completed steps
    - Step navigation works (click stepper, auto-advance on completion)
    - API calls wired from step components through to backend endpoints
    - Vite build succeeds with complete wizard app
  </done>
</task>

</tasks>

<verification>
- `cd supervisor/wizard && npx vite build` succeeds — full wizard app builds
- All components render their interaction states correctly per CONTEXT.md
- AUTH-04: Completed steps show checkmark + Sign Out
- AUTH-05: Alternative methods always visible
- SETUP-01: BuildProgress footer shows extension install progress
- SETUP-02: Stepper shows 3 steps with active/completed states
</verification>

<success_criteria>
- Complete wizard UI builds successfully with Vite
- All 4 requirement IDs (SETUP-01, SETUP-02, AUTH-04, AUTH-05) have corresponding UI implementations
- State machine handles all SSE event types and drives correct UI state transitions
- Visual style matches ClaudeOS dark theme palette
</success_criteria>

<output>
After completion, create `.planning/phases/12-wizard-ui-and-build-progress/12-02-SUMMARY.md`
</output>
