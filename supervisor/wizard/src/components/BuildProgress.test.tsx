import { describe, it } from "vitest";

describe("BuildProgress", () => {
  it.todo("renders progress bar during install");
  it.todo("shows completion state");
  it.todo("shows error with retry button");
});
