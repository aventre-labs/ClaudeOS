import { describe, it } from "vitest";

describe("RailwayStep", () => {
  it.todo("shows sign in button in idle state");
  it.todo("shows pairing code in pairing state");
  it.todo("shows completed state with sign out");
});
