import { describe, it } from "vitest";

describe("AnthropicStep", () => {
  it.todo("shows API key and login side by side");
  it.todo("shows completed state");
  it.todo("shows error with retry");
});
